
  # Hush to Hues

  This is a code bundle for Hush to Hues. The original project is available at https://www.figma.com/design/RHXcG8erzo4CbUHz8p3GSm/Hush-to-Hues.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  